---
phase: 01-foundation
plan: 02
type: execute
depends_on: ["01-01"]
files_modified: [src/_includes/base.njk, src/_data/site.json, src/index.njk]
---

<objective>
Create base layout template with Korean language support and site configuration.

Purpose: Establish the reusable layout foundation that all pages will extend, with proper Korean localization built in from the start.
Output: Base template with Korean meta tags, site data configuration, and index page using the layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./01-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**From Plan 01-01:**
- 11ty installed and configured
- src/ directory exists
- eleventy.config.js set up with Nunjucks templates

**Korean language requirements:**
- html lang="ko"
- meta charset="utf-8"
- Korean font considerations (system fonts fine for v1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create site data configuration</name>
  <files>src/_data/site.json</files>
  <action>
    1. Create src/_data/ directory
    2. Create site.json with:
       - title: "쿠팡 제품 리뷰" (Coupang Product Reviews)
       - description: "인기 쿠팡 제품에 대한 상세 리뷰와 추천" (Detailed reviews and recommendations)
       - language: "ko"
       - locale: "ko_KR"
       - url: "" (empty for now, will be GitHub Pages URL)
       - author: "Auto Blog"

    This data will be available globally in all templates as `site.*`
  </action>
  <verify>
    - src/_data/site.json exists and is valid JSON
    - `npx @11ty/eleventy --dryrun` still works (no syntax errors)
  </verify>
  <done>Site configuration data available for all templates</done>
</task>

<task type="auto">
  <name>Task 2: Create base layout template</name>
  <files>src/_includes/base.njk, src/index.njk</files>
  <action>
    1. Create src/_includes/ directory
    2. Create base.njk layout with:
       - DOCTYPE html
       - html lang="{{ site.language }}" (pulls from site.json)
       - head section:
         - meta charset="utf-8"
         - meta viewport for mobile
         - title: "{{ title }} | {{ site.title }}" with fallback
         - meta description using page description or site.description
       - body with {{ content | safe }} block
       - Basic semantic structure: header, main, footer placeholders

    3. Update src/index.njk to use the base layout:
       - Add front matter: layout: base.njk, title: "홈"
       - Keep content simple for now (h1 and intro paragraph in Korean)

    Keep the layout minimal — no styling yet. Focus on semantic HTML structure.
    Use Nunjucks filters like `| safe` for content to allow HTML rendering.
  </action>
  <verify>
    - src/_includes/base.njk exists with proper HTML structure
    - src/index.njk has layout front matter
    - `npx @11ty/eleventy` builds successfully
    - _site/index.html contains Korean title from site.json
  </verify>
  <done>Base layout created, index page using layout with Korean content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx @11ty/eleventy` builds without errors
- [ ] _site/index.html includes Korean title from site.json
- [ ] HTML has correct lang="ko" attribute
- [ ] Page title includes both page title and site title
- [ ] Meta description is present
</verification>

<success_criteria>

- Base layout template exists and is reusable
- Site configuration in _data/site.json
- Korean language properly set in HTML
- Index page renders using the layout
- All templates use Nunjucks consistently
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`:

Use summary template structure with:
- Accomplishments: Layout system established, Korean localization configured
- Files Created: List all new/modified files
- Decisions Made: Layout structure decisions, site data schema
- Issues Encountered: Any template issues
- Next Step: Ready for Phase 2 (SEO) or continue with 01-03
</output>
