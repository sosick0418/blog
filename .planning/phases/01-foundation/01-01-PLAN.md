---
phase: 01-foundation
plan: 01
type: execute
depends_on: []
files_modified: [package.json, eleventy.config.js, src/index.njk, .gitignore]
---

<objective>
Initialize 11ty static site generator with basic project structure.

Purpose: Create the foundation for the affiliate blog with a working 11ty setup that can build and serve pages.
Output: Functional 11ty project with npm configuration, eleventy config, and a basic index page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech context:**
- Starting from empty repository (only .planning/ exists)
- 11ty will be the static site generator
- JavaScript ecosystem throughout
- Must work with GitHub Pages deployment (Phase 7)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize npm project and install 11ty</name>
  <files>package.json, .gitignore</files>
  <action>
    1. Run `npm init -y` to create package.json
    2. Install 11ty: `npm install @11ty/eleventy --save-dev`
    3. Create .gitignore with: node_modules/, _site/, .DS_Store
    4. Update package.json name to "coupang-affiliate-blog" and remove default test script
  </action>
  <verify>
    - package.json exists with @11ty/eleventy in devDependencies
    - node_modules/@11ty/eleventy exists
    - .gitignore contains node_modules
  </verify>
  <done>npm project initialized, 11ty installed, git ignores configured</done>
</task>

<task type="auto">
  <name>Task 2: Create 11ty configuration and directory structure</name>
  <files>eleventy.config.js, src/index.njk</files>
  <action>
    1. Create eleventy.config.js with:
       - Input directory: "src"
       - Output directory: "_site"
       - Template formats: njk, md
       - Passthrough copy for assets (when added later)
    2. Create src/ directory
    3. Create src/index.njk with minimal HTML structure:
       - DOCTYPE html
       - html lang="ko" (Korean)
       - meta charset utf-8
       - title: "Coupang 제품 리뷰"
       - body with h1 heading and placeholder text

    Use Nunjucks (.njk) as primary template language — it's 11ty's default and most flexible.
    Avoid Liquid or other templating — stick to one for consistency.
  </action>
  <verify>
    - eleventy.config.js exists and exports a valid config function
    - src/index.njk exists with Korean lang attribute
    - `npx @11ty/eleventy --dryrun` shows no errors
  </verify>
  <done>11ty configured with src/ input, _site/ output, and working index page</done>
</task>

<task type="auto">
  <name>Task 3: Verify 11ty build works</name>
  <files>_site/index.html</files>
  <action>
    1. Run `npx @11ty/eleventy` to build the site
    2. Verify _site/index.html is generated
    3. Check the output HTML has Korean lang="ko" attribute preserved
    4. Run `npx @11ty/eleventy --serve` briefly to confirm dev server starts (then stop it)
  </action>
  <verify>
    - _site/index.html exists after build
    - File contains lang="ko" in html tag
    - Build completes without errors
  </verify>
  <done>11ty builds successfully and outputs valid HTML</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm list @11ty/eleventy` shows installed version
- [ ] `npx @11ty/eleventy` builds without errors
- [ ] _site/index.html exists and contains Korean content
- [ ] .gitignore prevents node_modules and _site from being committed
</verification>

<success_criteria>

- package.json exists with 11ty dependency
- eleventy.config.js properly configured
- src/index.njk template renders to _site/index.html
- Build process completes without errors
- Project structure ready for next plans to add layouts and styling
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

Use summary template structure with:
- Accomplishments: 11ty initialized, configured, verified working
- Files Created: List all new files
- Decisions Made: Template language choice (Nunjucks), directory structure
- Issues Encountered: Any setup issues
- Next Step: Ready for 01-02 (layout) and 01-03 (dev workflow)
</output>
