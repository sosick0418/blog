---
phase: 08-end-to-end-integration
plan: 02
type: execute
depends_on: ["08-01"]
files_modified: [n8n/workflows/content-pipeline.json]
---

<objective>
Deploy test post to live site and configure n8n workflow for production.

Purpose: Verify the complete GitHub Actions deployment pipeline and prepare n8n for automated operation.
Output: Live post published via automated pipeline, n8n workflow production-ready.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-end-to-end-integration/08-01-SUMMARY.md
@.github/workflows/deploy.yml
@n8n/workflows/content-pipeline.json

**Tech stack available:**
- GitHub Actions CI/CD workflow
- GitHub Pages at https://sosick0418.github.io/blog/
- n8n Docker setup with Execute Command node
- orchestrate-content.js with structured error codes

**Constraining decisions:**
- Phase 06: Schedule Trigger disabled until Phase 8 testing complete (06-02)
- Phase 07: Deployment triggers on push to main (07-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push test post to trigger deployment</name>
  <files></files>
  <action>Push the commits created in Plan 08-01 to trigger GitHub Actions deployment:

  ```bash
  git push origin main
  ```

  Monitor the GitHub Actions run to verify deployment completes successfully.
  Check the Actions tab or use: gh run list --limit 1</action>
  <verify>gh run list --limit 1 shows "completed" status, git log origin/main shows test post commits</verify>
  <done>Test post commits pushed, GitHub Actions deployment triggered and completed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Test post deployed to live GitHub Pages site</what-built>
  <how-to-verify>
    1. Wait for GitHub Actions to complete (~1-2 min)
    2. Visit https://sosick0418.github.io/blog/
    3. Verify test post(s) appear on homepage list
    4. Click on a test post
    5. Verify post renders correctly with:
       - Korean title and content
       - Product info section (price in Korean won format)
       - Affiliate link button
    6. Check browser console for any errors
    7. Test on mobile viewport (responsive layout)
  </how-to-verify>
  <resume-signal>Type "approved" if live deployment works, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update n8n workflow for production</name>
  <files>n8n/workflows/content-pipeline.json</files>
  <action>Update the n8n content-pipeline.json workflow:

  1. Remove --dry-run flag from Execute Command node (line with orchestrate-content.js)
  2. Update notes on Schedule Trigger to indicate it's ready for activation
  3. Keep Schedule Trigger disabled (user will enable manually when ready)

  The workflow should be production-ready but not automatically active.</action>
  <verify>cat n8n/workflows/content-pipeline.json | grep "dry-run" returns empty (no dry-run flag)</verify>
  <done>n8n workflow updated to production mode, ready for activation</done>
</task>

<task type="auto">
  <name>Task 3: Commit and push workflow update</name>
  <files>n8n/workflows/content-pipeline.json</files>
  <action>Commit and push the production-ready workflow:

  ```bash
  git add n8n/workflows/content-pipeline.json
  git commit -m "feat(08-02): configure n8n workflow for production

  - Remove --dry-run flag for live operation
  - Update schedule trigger notes for activation readiness
  - Workflow ready for production use"
  git push origin main
  ```</action>
  <verify>git log -1 shows the commit, git status shows clean working tree</verify>
  <done>Production workflow committed and pushed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test post visible on live site https://sosick0418.github.io/blog/
- [ ] Post renders correctly with Korean content and affiliate links
- [ ] n8n workflow updated without dry-run flag
- [ ] Workflow changes committed and pushed
- [ ] No console errors on live site
</verification>

<success_criteria>

- Full pipeline verified: product data → LLM → git → GitHub Actions → live site
- n8n workflow production-ready (disabled but configured correctly)
- Live site displays test posts correctly
- Ready for documentation phase (08-03)
</success_criteria>

<output>
After completion, create `.planning/phases/08-end-to-end-integration/08-02-SUMMARY.md`
</output>
